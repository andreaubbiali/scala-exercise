/**
    A class that represent a document written using the editor
*/

class Document(){
    /**
    var line = Array[Editor]()

    def addLine(l: String) = {
        line = line :+ l
    }

    def numLine(): Int = {
        return this.line.size
    }

    override def toString(): String = {
        return ""
    }
    */
}